# 2. Write a Python program that uses a custom iterator to iterate over a list of integers.

list = [10, 20, 30]
iterator = iter(list)

print(next(iterator))
print(next(iterator))
print(next(iterator))